import type { StyleProp } from "react-native";
import { PossiblyCompiledStyleProp } from "../common/Styles";
export interface DynamicStyleProcessorOptions {
    state?: string[];
}
export type DynamicStyleProcessor<T> = ((style: PossiblyCompiledStyleProp<T>, options?: DynamicStyleProcessorOptions) => StyleProp<T>);
interface ProcessDynamicStylesOutput<T> {
    classes: string[];
    requiresPostProcessing?: number[];
    result: T[];
}
export type Vars = Record<string, any>;
export interface DynamicStyleOptions {
    vars: Vars;
    baseFontSize?: number;
    optimise?: boolean;
    debug?: boolean;
}
export declare const processDynamicStyles: <T>(style: PossiblyCompiledStyleProp<T>, options: DynamicStyleOptions) => ProcessDynamicStylesOutput<T>;
export {};
