export interface TransformerOptions {
    filename: string;
    src: string;
    options: {
        platform: string;
        sassOptions: any;
    };
}
export interface Transformer {
    transform: (transformOptions: TransformerOptions) => Promise<any>;
}
export declare const createCssModulesTransformer: (upstreamTransformer: Transformer) => Transformer;
