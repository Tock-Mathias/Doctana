"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.extractVarFromNodes = void 0;
const extractVarFromNodes = (nodes) => {
    var _a;
    const varNode = nodes.find(node => node.type === "function" && node.value === "var");
    if (!varNode) {
        return undefined;
    }
    const wordNode = (_a = varNode.nodes) === null || _a === void 0 ? void 0 : _a[0];
    if (!wordNode) {
        return undefined;
    }
    return wordNode.type === "word" ? wordNode.value.substring(2) : undefined;
};
exports.extractVarFromNodes = extractVarFromNodes;
