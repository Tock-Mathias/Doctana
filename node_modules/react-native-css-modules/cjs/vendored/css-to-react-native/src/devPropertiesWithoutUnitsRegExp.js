"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
let propertiesWithoutUnits;
if (process.env.NODE_ENV !== 'production') {
    propertiesWithoutUnits = [
        'aspectRatio',
        'elevation',
        'flexGrow',
        'flexShrink',
        'opacity',
        'shadowOpacity',
        'zIndex',
    ];
}
const devPropertiesWithUnitsRegExp = propertiesWithoutUnits != null
    ? new RegExp(propertiesWithoutUnits.join('|'))
    : null;
exports.default = devPropertiesWithUnitsRegExp;
