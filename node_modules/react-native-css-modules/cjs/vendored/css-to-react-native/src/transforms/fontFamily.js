"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tokenTypes_1 = require("../tokenTypes");
exports.default = tokenStream => {
    let fontFamily;
    if (tokenStream.matches(tokenTypes_1.STRING)) {
        fontFamily = tokenStream.lastValue;
    }
    else {
        fontFamily = tokenStream.expect(tokenTypes_1.IDENT);
        while (tokenStream.hasTokens()) {
            tokenStream.expect(tokenTypes_1.SPACE);
            const nextIdent = tokenStream.expect(tokenTypes_1.IDENT);
            fontFamily += ` ${nextIdent}`;
        }
    }
    tokenStream.expectEmpty();
    return { fontFamily };
};
